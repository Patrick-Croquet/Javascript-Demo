// Déclaration d'une fonction simple
function saluer() {
    console.log("Bonjour à tous !");
  }
  
  // Appel de la fonction
  saluer();
  
  // Déclaration d'une fonction avec paramètres
  function additionner(x, y) {
    return x + y;
  }
  
  // Appel de la fonction avec passage d'arguments
  let resultat = additionner(5, 3);
  console.log("Résultat =", resultat); // Affiche 8
  
